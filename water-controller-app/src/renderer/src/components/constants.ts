export const errorDialogId = 'error-dialog'

/**
 * ログファイル読み込み時のデフォルト行数（末尾から）
 */
export const DEFAULT_LOG_TAIL_LINES = 100

/**
 * ログファイル読み込み時の最大行数（末尾から）
 * SettingsPanel の LogsTab で使用
 */
export const MAX_LOG_TAIL_LINES = 300
